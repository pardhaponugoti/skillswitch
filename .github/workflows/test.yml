name: Test

on:
  pull_request:
  push:
    branches: [main]

jobs:
  swift-test:
    name: swift test
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: swift test
